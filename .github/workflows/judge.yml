name: RobotJudge CI

on:
  push:
    paths:
      - 'submissions/**'
      - 'baselines/**'
      - 'robotjudge/**'
      - 'testcases/**'
      - 'configs/**'
      - 'schemas/**'
  pull_request:
    paths:
      - 'submissions/**'
      - 'baselines/**'
      - 'robotjudge/**'
      - 'testcases/**'
      - 'configs/**'
      - 'schemas/**'

jobs:
  unit:
    name: Unit Tests & Schema Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -e ".[dev]"

      - name: Run unit tests
        run: python -m pytest tests/ -v --tb=short

  judge:
    name: Judge — Baseline & Submissions
    needs: unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -e .

      # ------------------------------------------------------------------
      # Baseline sanity check
      # ------------------------------------------------------------------
      - name: Run baseline A* on public suite
        run: |
          python -m robotjudge.cli run \
            --suite testcases/public \
            --submission baselines/astar.py \
            --seeds 0..4 \
            --report-dir reports/baseline \
            --time-limit 30000

      - name: Verify baseline passes
        run: |
          python -c "
          import json, sys
          r = json.load(open('reports/baseline/results.json'))
          status = r['suite']['status']
          print(f'Baseline status: {status}')
          if status != 'PASS':
              print('ERROR: Baseline A* failed!', file=sys.stderr)
              sys.exit(1)
          print('Baseline OK ✅')
          "

      # ------------------------------------------------------------------
      # Additional Baseline Checks
      # ------------------------------------------------------------------
      - name: Run baseline BFS on public suite (quick)
        run: |
          python -m robotjudge.cli run \
            --suite testcases/public \
            --submission baselines/bfs.py \
            --seeds 0 \
            --report-dir reports/baseline_bfs \
            --time-limit 30000

      - name: Verify BFS passes
        run: |
          python -c "
          import json, sys
          r = json.load(open('reports/baseline_bfs/results.json'))
          status = r['suite']['status']
          print(f'BFS status: {status}')
          if status != 'PASS':
              print('ERROR: Baseline BFS failed!', file=sys.stderr)
              sys.exit(1)
          print('BFS OK ✅')
          "

      - name: Run baseline Dijkstra on public suite (quick)
        run: |
          python -m robotjudge.cli run \
            --suite testcases/public \
            --submission baselines/dijkstra.py \
            --seeds 0 \
            --report-dir reports/baseline_dijkstra \
            --time-limit 30000

      - name: Verify Dijkstra passes
        run: |
          python -c "
          import json, sys
          r = json.load(open('reports/baseline_dijkstra/results.json'))
          status = r['suite']['status']
          print(f'Dijkstra status: {status}')
          if status != 'PASS':
              print('ERROR: Baseline Dijkstra failed!', file=sys.stderr)
              sys.exit(1)
          print('Dijkstra OK ✅')
          "

      # ------------------------------------------------------------------
      # Determinism gate
      # ------------------------------------------------------------------
      - name: Determinism check (run 1)
        run: |
          python -m robotjudge.cli run \
            --suite testcases/public \
            --submission baselines/astar.py \
            --seeds 0..2 \
            --report-dir reports/det1 \
            --time-limit 30000

      - name: Determinism check (run 2)
        run: |
          python -m robotjudge.cli run \
            --suite testcases/public \
            --submission baselines/astar.py \
            --seeds 0..2 \
            --report-dir reports/det2 \
            --time-limit 30000

      - name: Compare determinism hashes
        run: python scripts/check_determinism.py reports/det1/results.json reports/det2/results.json

      # ------------------------------------------------------------------
      # Upload artifacts (always, even on failure)
      # ------------------------------------------------------------------
      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: robotjudge-reports
          path: reports/
          retention-days: 30
